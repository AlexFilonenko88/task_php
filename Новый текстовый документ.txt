1. Ассоциативный массив позволяет обращаться по ключу к данным; 
внути оного массива могут быть неопределнное количество вложенных массивов 9могут быть карточки товаров, пользователи ...), в каждом вложенном массиве свои ключи с данными к которым обращаемся для оттрисовки этих данных

2. потому что в верстке переаються сслки на ключи из массива, поменяв структуру поломаем верстку

3. объявление массива [] (новая запись), array() (старая запись)
в массиве могут храниться сисла строки, буль
формиуеться начинаюс 0 (индексы массива)
в верстке если мне нужно отрисовть поля name, в массиве ключь назову аналогично, что бы был понятный смысл

4. единая структура блока необходима для простоты обслуживания кода: в одном цикле foreach один тип разметки для отрисовки данных, без дополнительных условий для отрисоки другого участка кода

5. div блочный элемент, поэтому дам ему style=”padding: 40px;” для правильноговизуального отображения содержимого на странице


6. 
	1. когда пишем на чистом php
	2. когда используем php в верстке

7. условие ? выполниться если true : выполниться  если false;
пример: <? echo $user["is_banned"] ? "banned" : "" ?>
тернарный оператор болеее короче в своей записи 
 
